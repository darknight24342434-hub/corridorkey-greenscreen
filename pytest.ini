[pytest]
# run_corridorkey_test.py is a runner script, not a test module, but its filename
# matches pytest's default *_test.py pattern and importing it pulls in torch.
# Collect only from tests/.
testpaths = tests
